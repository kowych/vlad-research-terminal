import EmptyArchive from "@/components/EmptyArchive";
import PageHeader from "@/components/PageHeader";
import { positions } from "@/lib/content";
export const metadata = { title: "Positions" };
export default function PositionsPage() { return <section className="page-shell"><PageHeader number="03" eyebrow="PUBLIC POSITION BOOK" title="Positions" description="Public market views with a defined horizon, risk budget and invalidation. Performance is not published." /><div className="mt-10">{positions.length ? <div className="position-table"><div className="position-head"><span>INSTRUMENT</span><span>VIEW</span><span>HORIZON</span><span>RISK</span><span>STATUS</span></div>{positions.map((position) => <div key={`${position.instrument}-${position.direction}`} className="position-row"><div><strong>{position.instrument}</strong><p>{position.thesis}</p></div><span>{position.direction}</span><span>{position.horizon}</span><span>{position.risk}</span><span>{position.status}</span><p className="position-invalidation">INVALIDATION · {position.invalidation}</p></div>)}</div> : <EmptyArchive label="POSITION BOOK EMPTY" detail="When a position is published, this page will disclose the instrument, directional view, time horizon, percentage risk and invalidation—without realised or unrealised P/L." />}</div></section>; }
